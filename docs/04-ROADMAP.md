# 04 — Roadmap (порядок работы для Claude Code)

Работать строго по фазам. Каждая фаза заканчивается работающим, проверяемым результатом.

## Фаза 0 — Проверка армянского языка (блокирующая) ✅ ПРОЙДЕНА (2026-07-14)

Цель: снять главный риск проекта до написания продукта.

- [x] Скрипт: взяты 3 реальных армянских документа заказчика, прогнаны через
      Docling → 494 чанка (harness: `apps/ingest/phase0/`).
- [x] Составлены 25 вопросов с известным целевым чанком (+5 ловушек).
- [~] Замер recall для `bge-m3` выполнен; `text-embedding-3-large` **не сравнивался**
      (нулевая квота OpenAI — повторить при необходимости).
- [x] Замерен вклад BM25 (стеммер — заглушка, заменить в проде) и reranker
      `bge-reranker-v2-m3` (подтверждён: +0.12 R@1, +0.10 MRR).
- [x] Решение зафиксировано в `docs/02-ARCHITECTURE.md` (таблица «Армянский язык»).

**Критерий выхода:** hybrid+rerank recall@5 ≥ 0.85 — **✅ достигнут (0.96)** на
`bge-m3`. Отчёт: `apps/ingest/phase0/reports/phase0_report.md`.
Открытые хвосты (не блокируют Фазу 2): вектор Qdrant 3072→1024 под `bge-m3`,
полноценный армянский стеммер вместо заглушки, review вопросов носителем.

## Фаза 1 — Каркас (неделя 1)

- [ ] Monorepo (pnpm workspaces): apps/web, apps/api, apps/ingest, packages/shared, packages/ui.
- [ ] docker-compose: postgres, redis, qdrant, minio.
- [ ] NestJS: auth (JWT+refresh), users, RBAC-guard, rate limiting, audit_log.
- [ ] Next.js: shadcn init по `03-DESIGN-SYSTEM.md` (токены, шрифты Noto Armenian,
      светлая+тёмная темы), layout, страница логина, i18n-словарь hy.
- [ ] Миграции всех таблиц из `02-ARCHITECTURE.md`.

## Фаза 2 — Ингестия документов (неделя 2)

- [x] ingest-сервис (ядро): Docling-парсинг PDF/DOCX + Excel «строка+шапка+лист»
      (openpyxl), semantic chunking, эмбеддинги `bge-m3`, Qdrant upsert + BM25.
      Проверено на реальном документе + тесты на qdrant `:memory:`
      (`apps/ingest/app/pipeline/`, `apps/ingest/tests/`).
- [x] Удаление и переиндексация документов из админки: чанки из Qdrant+BM25,
      файл с диска, строка БД (+источники старых сообщений), кэш-инвалидация,
      audit. Ингестия идемпотентна (старые точки документа чистятся до upsert);
      reindex/delete при processing → 409 (гонка параллельной ингестии — до
      BullMQ). Проверено live-циклом upload→409→ready→reindex→delete.
- [ ] Гранулярность таблиц: большие markdown-таблицы резать построчно (поиск по
      ячейке; выявлено при верификации — суточные по стране не всегда в top-5).
- [ ] Точные номера страниц (Docling) — сейчас `page=None` для PDF/DOCX.
- [ ] BullMQ-пайплайн со статусами; SSE прогресса в админку. **(нужен Docker/Redis)**
- [ ] Админка Documents: таблица, dropzone-загрузка, статусы, замена (теневая
      коллекция + alias switch), удаление, переиндексация. Все 4 состояния экрана.
      **(нужен api + Docker)**

> ⚠️ Полный стек (BullMQ, Postgres-статусы, MinIO, интеграция с api, админка)
> заблокирован установкой **Docker** (CLI не найден в среде). Ядро ингестии
> построено и проверено оффлайн через локальный режим Qdrant.

## Фаза 3 — Чат и RAG-пайплайн (недели 3–4)

- [x] API (backend): hybrid search → RRF → rerank → двухпороговый guardrail →
      context builder → LLM (SSE-стрим) → сохранение message + sources.
      Проверено e2e: релевантный вопрос → high + источники; off-topic → refused
      без LLM. LLM-провайдер: openai/anthropic/**stub** (работает без ключа).
- [x] Query rewriting: follow-up переписывается LLM в самостоятельный вопрос
      с учётом истории (проверено: «Իսկ նվազագույնը…» → полный вопрос → верный
      ответ). Stub-провайдер — pass-through.
- [x] Redis-кэш ответов: нормализованный переписанный вопрос → ответ+источники;
      инвалидация версией kb:version при изменении документов. Кэш-хит ~0.1s
      против ~38s полного прогона; cached-флаг в message.
- [x] Токен-метрики: tokensIn/tokensOut из usage провайдера сохраняются в БД.
- [~] Остаток: grounding-проверка ответа (базовая — через промпт).
- [x] UI чата по `03-DESIGN-SYSTEM.md`: SSE-стриминг с курсором, цитатные чипы ⟨n⟩
      (клик→скролл к источнику), карточки источников, кромка уверенности, отказ
      как спокойная карточка, пустой чат с примерами, история чатов. CORS для SSE.
- [~] Хвосты UI: HoverCard-превью чипа (сейчас title), мобильный Sheet сайдбара.

> Для реальных ответов (не stub) нужен ключ LLM: `OPENAI_API_KEY` (с балансом)
> или `ANTHROPIC_API_KEY`. Retrieval бесплатен (bge-m3).

## Фаза 4 — Evaluation и калибровка (неделя 5)

- [ ] Генератор синтетического датасета (см. `05-EVALUATION.md`): 150–200 Q/A/source.
- [ ] Скрипт прогонов: recall, точность порогов, % ложных отказов / ложных ответов.
- [ ] Подобрать THRESHOLD_LOW / THRESHOLD_HIGH; зафиксировать в env и в этом файле.
- [ ] Мини-review датасета админом (страница в админке: approve/reject вопросов).

## Фаза 5 — Аналитика и полировка (неделя 6)

- [ ] Dashboard: клиенты, документы, вопросы/день, % отказов, % low-confidence,
      токены, cache hit rate.
- [ ] Популярные вопросы; экспорт CSV.
- [ ] Клиенты: CRUD + блокировка; Audit-журнал.
- [ ] e2e-тесты happy path; нагрузочный smoke (50 параллельных вопросов).
- [ ] Прогон интерфейса с носителем армянского: тексты, длины строк, переносы.

## Definition of Done (каждая задача)

Типы strict · валидация входа · RBAC · состояния loading/empty/error ·
строки в i18n · unit-тесты логики · токены дизайна, без инлайн-стилей.
