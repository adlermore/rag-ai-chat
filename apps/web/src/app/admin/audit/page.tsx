import { ComingSoon } from "@/components/admin/coming-soon";
import { t } from "@/lib/i18n";

// Аудит-журнал — Фаза 5. Пока плейсхолдер (данные пишутся в audit_log уже сейчас).
export default function AuditPage() {
  return <ComingSoon title={t("nav.audit")} />;
}
