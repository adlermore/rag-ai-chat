# apps/ingest — Фаза 2: продовая ингестия.
# Версии выверены в Фазе 0 (apps/ingest/phase0) на реальных армянских документах.

# ── Веб-слой ──
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12      # multipart-загрузка файлов
python-dotenv==1.0.1

# ── Парсинг документов ──
docling==2.8.1
docling-core==2.5.1           # docling не пинит жёстко; свежие ломают импорт
openpyxl==3.1.5               # Excel: построчный чанкинг (строка + шапка + лист)

# ── Embeddings ──
# Основной путь (валидирован в Фазе 0): self-hosted bge-m3 (dim 1024) + reranker.
FlagEmbedding==1.3.2          # bge-m3, bge-reranker-v2-m3 (CPU); тянет torch
# Опционально для сравнения/перехода на OpenAI text-embedding-3-large:
openai==1.54.4
httpx==0.27.2                 # openai 1.54.x несовместим с httpx>=0.28 (аргумент proxies)

# ── ONNX-реранкер (int8, CPU 1.5–4×; RERANKER_BACKEND=onnx-int8) ──
optimum[onnxruntime]==1.23.3
onnxscript==0.7.1       # нужен torch>=2.9 для ленивого ONNX-экспорта при первом старте

# ── Хранилища индекса ──
qdrant-client==1.12.1         # dense-вектор; поддерживает локальный режим (:memory:/path)
rank-bm25==0.2.2              # BM25 Okapi (армянская токенизация — app/pipeline/tokenizer_hy.py)

# ── Разработка ──
pytest==8.3.3
