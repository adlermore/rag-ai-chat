# Зависимости harness'а Фазы 0 для РЕАЛЬНЫХ прогонов.
# Ядро harness (метрики, RRF, BM25, dense-cosine) — pure-python и работает
# БЕЗ этих пакетов (smoke-тест: python run_all.py --use-fixtures).

# Ингестия документов
docling==2.8.1
# docling НЕ пинит docling-core жёстко (>=2.5.1,<3.0.0); свежие 2.8x ломают
# импорт (BoundingBox убран из legacy_doc.base). Пиним на совместимую версию.
docling-core==2.5.1

# Embeddings — вариант A: OpenAI text-embedding-3-large (основной кандидат)
openai==1.54.4

# Embeddings — вариант B: self-hosted bge-m3 + reranker bge-reranker-v2-m3
FlagEmbedding==1.3.2
# (тянет torch; на CPU достаточно, но образ тяжёлый)

# Точный подсчёт токенов при чанкинге (иначе — приблизительно по словам)
tiktoken==0.8.0

# Загрузка .env
python-dotenv==1.0.1
